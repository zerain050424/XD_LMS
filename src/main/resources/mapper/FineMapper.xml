<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group3.xd_lms.mapper.FineMapper">

    <resultMap id="BaseResultMap" type="com.group3.xd_lms.entity.Fine">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="borrow_record_id" property="borrowRecordId"/>
        <result column="amount" property="amount"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="paid_at" property="paidAt"/>
    </resultMap>

    <insert id="insertFine" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO fines (user_id, borrow_record_id, amount, status, created_at)
        VALUES (#{userId}, #{borrowRecordId}, #{amount}, #{status}, NOW())
    </insert>

    <select id="selectFinesByUserId" resultMap="BaseResultMap">
        SELECT * FROM fines WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <update id="updatePaymentStatus">
        UPDATE fines
        SET status = #{status}, paid_at = CASE WHEN #{status} = 'Paid' THEN NOW() ELSE NULL END
        WHERE id = #{fineId}
    </update>

    <select id="sumUnpaidFinesByUserId" resultType="java.math.BigDecimal">
        SELECT IFNULL(SUM(amount), 0) FROM fines WHERE user_id = #{userId} AND status = 'Unpaid'
    </select>

</mapper>