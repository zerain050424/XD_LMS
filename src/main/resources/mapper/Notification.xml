<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group3.xd_lms.mapper.NotificationMapper">

    <resultMap id="BaseResultMap" type="com.group3.xd_lms.entity.Notification">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="type" property="type"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="is_read" property="isRead"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <insert id="insertNotification" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO notifications (user_id, type, title, content, is_read, created_at)
        VALUES (#{userId}, #{type}, #{title}, #{content}, #{isRead}, NOW())
    </insert>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT * FROM notifications WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <update id="markAsRead">
        UPDATE notifications SET is_read = 1 WHERE id = #{id}
    </update>

</mapper>